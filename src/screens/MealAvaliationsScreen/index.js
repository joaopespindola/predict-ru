import React from "react";
import { Container, MealsList } from "../../components";

export const MealAvaliationsScreen = () => {
    return (
        <Container align = "center" justify = "center">
            <MealsList />
        </Container>
    );
}